local Instance = require("@Instance")
local InternalStorage = Instance.new("InternalStorage")
InternalStorage.ExplorerHidden = true

-- this is used for internal storage of the engine, such as remote events or storing internal assets

return InternalStorage
