return function(renderer)
    return {
        init = renderer.InitWindow,
        close = renderer.CloseWindow,
        shouldClose = renderer.WindowShouldClose,
        isReady = renderer.IsWindowReady,
        isFullscreen = renderer.IsWindowFullscreen,
        isHidden = renderer.IsWindowHidden,
        isMinimized = renderer.IsWindowMinimized,
        isMaximized = renderer.IsWindowMaximized,
        isFocused = renderer.IsWindowFocused,
        isResized = renderer.IsWindowResized,
        isState = renderer.IsWindowState,
        setState = renderer.SetWindowState,
        clearState = renderer.ClearWindowState,
        toggleFullscreen = renderer.ToggleFullscreen,
        toggleBorderlessWindowed = renderer.ToggleBorderlessWindowed,
        maximize = renderer.MaximizeWindow,
        minimize = renderer.MinimizeWindow,
        restore = renderer.RestoreWindow,
        setIcon = renderer.SetWindowIcon,
        setIcons = renderer.SetWindowIcons,
        setTitle = renderer.SetWindowTitle,
        setPosition = renderer.SetWindowPosition,
        setMonitor = renderer.SetWindowMonitor,
        setMinSize = renderer.SetWindowMinSize,
        setMaxSize = renderer.SetWindowMaxSize,
        setSize = renderer.SetWindowSize,
        setOpacity = renderer.SetWindowOpacity,
        setFocused = renderer.SetWindowFocused,
        getHandle = renderer.GetWindowHandle,
        getScreenWidth = renderer.GetScreenWidth,
        getScreenHeight = renderer.GetScreenHeight,
        getRenderWidth = renderer.GetRenderWidth,
        getRenderHeight = renderer.GetRenderHeight,
        getPosition = renderer.GetWindowPosition,
        getScaleDPI = renderer.GetWindowScaleDPI,
        getMonitorCount = renderer.GetMonitorCount,
        getCurrentMonitor = renderer.GetCurrentMonitor,
        getMonitorPosition = renderer.GetMonitorPosition,
        getMonitorWidth = renderer.GetMonitorWidth,
        getMonitorHeight = renderer.GetMonitorHeight,
        getMonitorPhysicalWidth = renderer.GetMonitorPhysicalWidth,
        getMonitorPhysicalHeight = renderer.GetMonitorPhysicalHeight,
        getMonitorRefreshRate = renderer.GetMonitorRefreshRate,
        getMonitorName = renderer.GetMonitorName,
        setClipboardText = renderer.SetClipboardText,
        getClipboardText = renderer.GetClipboardText,
        getClipboardImage = renderer.GetClipboardImage,
        enableEventWaiting = renderer.EnableEventWaiting,
        disableEventWaiting = renderer.DisableEventWaiting,
        clearBackground = renderer.ClearBackground,
        beginDrawing = renderer.BeginDrawing,
        endDrawing = renderer.EndDrawing,
        setTargetFPS = renderer.SetTargetFPS,
        getFrameTime = renderer.GetFrameTime,
        getTime = renderer.GetTime,
        getFPS = renderer.GetFPS,
        swapScreenBuffer = renderer.SwapScreenBuffer,
        pollInputEvents = renderer.PollInputEvents,
        waitTime = renderer.WaitTime,
        showCursor = renderer.ShowCursor,
        hideCursor = renderer.HideCursor,
        enableCursor = renderer.EnableCursor,
        disableCursor = renderer.DisableCursor,
        isCursorHidden = renderer.IsCursorHidden,
        isCursorOnScreen = renderer.IsCursorOnScreen,
        takeScreenshot = renderer.TakeScreenshot,
        setConfigFlags = renderer.SetConfigFlags,
        openURL = renderer.OpenURL,
    }
end